/*
 * bw.c
 *
 *  Created on: 13 мар. 2023 г.
 *      Author: wooffie
 */

#include "bw.h"
#include <math.h>
//
//void bw_init(int16_t order, float freq, float upper, float lowwer) {
//	// order must div 4
//	double a = cosf(M_PI * (upper + lowwer) / freq)
//			/ cosf(M_PI * (upper - lowwer) / freq);
//	double a2 = a * a;
//	double b = tanf(M_PI * (upper - lowwer) / freq);
//	double b2 = b * b;
//	double r;
//
//
//
//
//}
